import React from 'react';
import { Text } from 'react-native';

export default ({ text }) => (
	<Text style={{ fontSize: 45, marginBottom: 15, fontWeight: 'bold', color: '#007ac1' }}>{text}</Text>
);
